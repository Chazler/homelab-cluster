kyverno:
  fullnameOverride: kyverno

  admissionController:
    rbac:
      clusterRole:
        extraResources:
          - apiGroups:
              - gateway.networking.k8s.io
            resources:
              - httproutes
            verbs:
              - get
              - list
              - watch
          - apiGroups:
              - gateway.envoyproxy.io
            resources:
              - securitypolicies
            verbs:
              - get
              - list
              - watch
              - create
              - update
              - patch
              - delete

  backgroundController:
    rbac:
      clusterRole:
        extraResources:
          - apiGroups:
              - gateway.networking.k8s.io
            resources:
              - httproutes
            verbs:
              - get
              - list
              - watch
          - apiGroups:
              - gateway.envoyproxy.io
            resources:
              - securitypolicies
            verbs:
              - get
              - list
              - watch
              - create
              - update
              - patch
              - delete

policies:
  authentikForwardAuth:
    enabled: true
    # HTTPRoutes with this label receive an Authentik external-auth policy and
    # a public route for the provider's outpost endpoints.
    labelKey: authentik-forward-auth
    labelValue: "true"
    authentikNamespace: authentik
    authentikServiceName: ak-outpost-homelab-forward-auth
    authentikServicePort: 9000
